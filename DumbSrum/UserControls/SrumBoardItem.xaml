<UserControl x:Class="DumbSrum.UserControls.SrumBoardItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DumbSrum.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200" MouseDoubleClick="UserControl_MouseDoubleClick"
             ToolTip="Double-click to see more details">
    <Grid Background="White" Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition />
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
            <Label x:Name="lblTaskID" Content="{Binding TaskID}"/>
            <Label Content="{Binding FeatureName}"/>
        </StackPanel>
        
        <TextBlock Grid.Row="1" Grid.ColumnSpan="3" Text="{Binding Story}" FontSize="10" Margin="5" TextWrapping="Wrap"/>

        <Button Grid.Row="2" x:Name="btnStart" Content="Start" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="10" Margin="0,0,8,0" Padding="10,0"/>
    </Grid>
</UserControl>
