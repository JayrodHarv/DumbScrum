<UserControl x:Class="DumbSrum.Views.ProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DumbSrum.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="0,0,1,0">
            <StackPanel Orientation="Vertical" Background="#FFEAE9E2">
                <RadioButton Content="Dashboard" Height="50" IsChecked="True" Style="{StaticResource ProjectButtonTheme}"/>
                <RadioButton Content="Feed" Height="50" Style="{StaticResource ProjectButtonTheme}"/>
                <RadioButton Content="Backlog" Height="50" Style="{StaticResource ProjectButtonTheme}"/>
                <RadioButton Content="Board" Height="50" Style="{StaticResource ProjectButtonTheme}"/>
            </StackPanel>
        </Border>
        <ContentControl Grid.Column="1" Content="{Binding CurrentProjectView}">
            
        </ContentControl>
    </Grid>
</UserControl>
